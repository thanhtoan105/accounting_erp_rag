<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="001-1-create-extensions-schema" author="dev-agent">
        <comment>
            Ensure the extensions schema exists so pgvector can be installed outside public schema.
            Story 1.3: AC1 - Enable pgvector extension via managed migrations.
        </comment>

        <sql>CREATE SCHEMA IF NOT EXISTS extensions;</sql>
    </changeSet>

    <changeSet id="001-2-enable-pgvector-extension" author="dev-agent">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM pg_extension WHERE extname = 'vector'
            </sqlCheck>
        </preConditions>

        <comment>
            Enable pgvector extension and retain it after Supabase upgrades.
            Story 1.3: AC1 - Liquibase-managed CREATE EXTENSION vector.
        </comment>

        <sql>CREATE EXTENSION IF NOT EXISTS vector WITH SCHEMA extensions;</sql>
    </changeSet>

</databaseChangeLog>
