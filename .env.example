# Supabase Database Connection
# Copy file này thành .env và điền thông tin thật
# Lấy connection string từ: Dashboard → Settings → Database → Connection String

# Connection Pooler (RECOMMENDED - Dùng cho production)
# Format: aws-[region].pooler.supabase.com hoặc [region]-0.pooler.supabase.com
# Ghi chú: Pooler sử dụng cổng 6543 (transaction mode). Script setup sẽ tự chọn nếu host là *.pooler.supabase.com
SUPABASE_HOST="aws-1-us-east-2.pooler.supabase.com"
SUPABASE_PORT="6543"
SUPABASE_DATABASE="postgres"

# User Credentials
# Username format: postgres.YOUR_PROJECT_REF
SUPABASE_USERNAME="postgres.YOUR_PROJECT_REF"
SUPABASE_DB_PASSWORD="YOUR_PASSWORD_HERE"

# Admin User (giống như user thường trong Supabase)
SUPABASE_ADMIN_USERNAME="postgres.YOUR_PROJECT_REF"
SUPABASE_ADMIN_PASSWORD="YOUR_PASSWORD_HERE"

# Direct Connection (Alternative - chỉ dùng khi cần thiết)
# Nếu muốn dùng direct connection thay vì pooler:
# SUPABASE_HOST="db.YOUR_PROJECT_REF.supabase.co"
# SUPABASE_PORT="5432"

# Application Settings
SPRING_PROFILES_ACTIVE="supabase"
SERVER_PORT="8080"

# Schema Documentation
SCHEMA_NAME="accounting"
SCHEMA_OUTPUT_DIR="docs/database"
